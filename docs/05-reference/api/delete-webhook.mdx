---
title: "DELETE webhook"
---

# `DELETE` webhook

`DELETE /webhook/{webhookID}` deletes an existing `webhook` object.

```bash
curl -X DELETE 'https://livepeer.studio/api/webhook/{webhookID}' \
-H 'authorization: Bearer {api-key}'
```

204 No Content
